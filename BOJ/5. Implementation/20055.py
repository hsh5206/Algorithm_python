# 컨베이어 벨트 위의 로봇
# 컨베이어 벨트 위의 로봇
N, K = map(int, input().split())
arr = list(map(int, input().split()))
robot = [0] * (2*N)
# 0 >> 올리는 위치, N-1 >> 내리는 위치
print(arr)

while True:
    robot[i] = 1

    i += 1
